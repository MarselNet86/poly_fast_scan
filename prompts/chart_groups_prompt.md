# Промпт для создания графиков дашборда Polymarket 15-min Markets

Ты создаёшь интерактивный веб-дашборд для мониторинга 15-минутных бинарных опционов BTC на Polymarket. Данные поступают из CSV-логов с частотой ~1 запись/секунду. Ось X на всех графиках — `seconds_till_end` (900→0, время до конца рынка). Данные приходят из трёх источников: Chainlink (оракул — определяет исход рынка), Binance (биржа — опережающий индикатор цены), Polymarket (стаканы заявок UP/DOWN контрактов).

---

## Группа 0: Header — сводная панель состояния рынка

**Переменные:** `timestamp_et`, `seconds_till_end`, `time_till_end`, лучшие аски UP и DOWN (из `up_ask_1_price`, `down_ask_1_price`)

**Описание:**
Верхняя строка-панель (не график, а info bar), всегда видна. Отображает: текущее время ET, обратный отсчёт до конца рынка (MM:SS + секунды), текущие лучшие цены покупки контрактов PM_UP и PM_DN. Дополнительно показывать сумму UP+DN как индикатор эффективности рынка.

**Торговый контекст:**
- `PM_UP + PM_DN ≈ 1.00` → рынок эффективен
- `PM_UP + PM_DN < 0.98` → арбитражная возможность (редко)
- `PM_UP + PM_DN > 1.02` → повышенные спреды, низкая ликвидность
- PM_UP = 0.55 означает: рынок оценивает вероятность роста BTC в 55%

**Фон панели меняет цвет по фазам рынка:**
- 🟢 Зелёный (>600s / 10+ мин) — ранняя фаза, цены волатильны
- 🟡 Жёлтый (300–600s / 5–10 мин) — формирование тренда, основная фаза входа
- 🟠 Оранжевый (120–300s / 2–5 мин) — развязка, фиксация прибыли
- 🔴 Красный (<120s / <2 мин) — финал, только экстренный выход
- ⚫ Мигающий красный (<30s) — критическая зона, ликвидность минимальна

---

## Группа 1: Ценовой график (Price & Oracle)

**Переменные:** `binance_btc_price`, `oracle_btc_price`, `lag`

**Описание графика:**
Двухлинейный график с наложением. Основная линия — цена Binance BTC/USDT (непрерывная, обновляется каждую секунду). Вторая линия — цена оракула Chainlink (может появляться с задержкой, иметь пропуски в начале рынка). Внизу подграфик (subplot) или цветная полоса, отображающая `lag` — разницу между оракулом и Binance. Положительный lag (оракул выше) — зелёный, отрицательный (Binance лидирует) — красный.

**Торговый контекст:**
Именно цена оракула определяет исход рынка (UP/DOWN). Lag показывает арбитражные окна: если Binance ушёл вниз, а оракул ещё не обновился — окно для покупки DOWN.

**Особенности:**
- Ось Y: цена в USD, масштаб автоматический
- `oracle_btc_price` может быть пустым — рисовать только при наличии данных
- Подсветить горизонтальной линией цену оракула на момент открытия рынка (strike price)
- Опционально: наложить `binance_vwap_30s` как пунктирную линию (VWAP — ключевой уровень для крупных игроков, пробой VWAP с объёмом = сильный сигнал)

---

## Группа 2: Моментум и доходность (Returns / Momentum)

**Переменные:** `binance_ret1s_x100`, `binance_ret5s_x100`

**Описание графика:**
Двухлинейный осциллятор вокруг нулевой линии. `ret1s` — быстрый сигнал (тонкая линия или точки), `ret5s` — сглаженный тренд (толстая линия). Значения в базисных пунктах (0.01 = 1 bp = 0.01%).

**Торговый контекст и пороги:**
- `|Ret1s| < 0.01` → спокойный рынок, можно ставить лимитные ордера
- `|Ret1s| > 0.05` → импульс, возможен пробой
- `Ret1s > 0.10` → сильный импульс вверх — сигнал для UP
- `Ret1s < -0.10` → сильный импульс вниз — сигнал для DOWN
- `|Ret5s| > 0.20` → значимое движение за 5 секунд
- **Ret1s и Ret5s одного знака** → подтверждённый тренд
- **Ret1s и Ret5s разных знаков** → разворот или консолидация
- **Ret1s резко вырос, Ret5s ещё низкий** → начало импульса

**Особенности:**
- Нулевая горизонтальная линия обязательна
- Пороговые зоны ±0.05 (пунктир) и ±0.10 (выделенный фон: зелёный/красный)
- Ось Y: базисные пункты (bp)

---

## Группа 3: Объёмы и всплески (Volume & Spikes)

**Переменные:** `binance_volume_1s`, `binance_volume_5s`, `binance_volma_30s`, `binance_volume_spike`

**Описание графика:**
Комбинированный график из двух частей. **Верхняя часть** — столбчатая диаграмма (bar chart) `binance_volume_1s` (объём за секунду, USDT), наложенная линия `binance_volma_30s` (скользящее среднее 30с) для контекста. Можно также отобразить `binance_volume_5s` как более толстую полупрозрачную полосу. **Нижняя часть** — линия `binance_volume_spike` (коэффициент всплеска).

**Торговый контекст и пороги:**
- Типичный V1s для BTCUSDT: $50,000–$500,000
- `V1s < $50,000` → низкая активность
- `V1s > $500,000` → повышенная активность
- `V1s > $1,000,000` → крупный игрок или новость
- `Spike < 0.5` → затишье, ниже среднего
- `Spike 0.5–2.0` → нормальная активность
- `Spike 2.0–3.0` → повышенная активность
- `Spike > 3.0` → значительный всплеск — **СИГНАЛ**
- `Spike > 5.0` → экстремальный всплеск — новость или крупный игрок
- **Spike > 3 + Ret1s > 0.05** → сильный сигнал UP
- **Spike > 3 + Ret1s < -0.05** → сильный сигнал DOWN
- **Spike > 3 без движения цены** → ловушка / скрытое накопление

**Особенности:**
- Бары volume_1s окрашены по spike: <1 серый, 1–2 жёлтый, 2–3 оранжевый, >3 красный
- Горизонтальные пороги на subplot spike: 1.0, 2.0, 3.0 (пунктир), 5.0 (красный пунктир)
- Ось Y верхняя: USDT; ось Y нижняя: безразмерный коэффициент

---

## Группа 4: Волатильность (Volatility)

**Переменные:** `binance_atr_5s`, `binance_atr_30s`, `binance_rvol_30s`

**Описание графика:**
Двухуровневый график. **Верхний** — две линии ATR: быстрый `atr_5s` и медленный `atr_30s` (в USD). **Нижний** — `rvol_30s` (реализованная волатильность в %).

**Торговый контекст и пороги:**
- `ATR5s < 5` → низкая волатильность, спокойный рынок
- `ATR5s 5–15` → нормальная волатильность
- `ATR5s > 20` → высокая волатильность, осторожность с размером позиции
- `ATR5s > 2 × ATR30s` → рынок "проснулся", ожидай продолжения движения
- `ATR30s >> ATR5s` → волатильность снижается (затишье)
- `RVol < 0.01%` → очень низкая волатильность
- `RVol 0.01–0.05%` → нормальная
- `RVol > 0.10%` → высокая, возможен пробой уровня

**Особенности:**
- Ось Y верхняя: USD (абсолютный диапазон)
- Ось Y нижняя: % (процент)
- Заливка между atr_5s и atr_30s: зелёная когда atr_5s > atr_30s (расширение), красная когда atr_5s < atr_30s (сжатие)
- Пороги ATR: горизонтальные линии 5, 15, 20
- Порог RVol: горизонтальная линия 0.10%

---

## Группа 5: VWAP — отклонение цены от средневзвешенной (VWAP Deviation)

**Переменные:** `binance_vwap_30s`, `binance_p_vwap_5s`, `binance_p_vwap_30s`

**Описание графика:**
Осциллятор вокруг нуля. Две линии: `p_vwap_5s` (быстрое отклонение, %) и `p_vwap_30s` (медленное отклонение, %). Пересечение линий — потенциальный сигнал смены направления.

**Торговый контекст:**
- `P/VWAP > 0` → цена выше VWAP — покупательское давление (бычий сигнал)
- `P/VWAP < 0` → цена ниже VWAP — давление продавцов (медвежий сигнал)
- `|P/VWAP5s| > 0.05%` → значимое отклонение
- **P/VWAP5s и P/VWAP30s > 0** → подтверждённый бычий тренд
- **P/VWAP5s > 0, P/VWAP30s < 0** → возможный разворот вверх
- **P/VWAP5s < 0, P/VWAP30s > 0** → возможный разворот вниз
- **Дивергенция P/VWAP5s и P/VWAP30s** → сигнал разворота

**Особенности:**
- Нулевая линия обязательна
- Ось Y: %
- Заливка: зелёная выше 0, красная ниже 0
- Вертикальные маркеры в точках пересечения p_vwap_5s и p_vwap_30s (сигнал разворота)

---

## Группа 6: Индикатор запаздывания оракула (Latency Direction)

**Переменные:** `lat_dir_raw_x1000`, `lat_dir_norm_x1000`

**Описание графика:**
Осциллятор вокруг нуля. Показывает, "ведёт" ли Binance оракул в определённом направлении. `raw` — сырой сигнал (lag × ret1s × 1000), `norm` — нормализованный ((lag / oracle_price) × ret1s × 1000).

**Торговый контекст:**
Ключевой индикатор для late-entry стратегии. Если Binance уже двинулся, а оракул ещё нет — есть окно для ставки. Положительные значения указывают на арбитражное окно вверх, отрицательные — вниз.

**Особенности:**
- Может быть пустым в начале рынка — показать плашку "Ожидание данных оракула"
- Ось Y: безразмерные единицы ×1000
- Подсветка зон: зелёная при сильных положительных значениях, красная при сильных отрицательных

---

## Группа 7: Стакан ордеров — визуализация (Orderbook Visualization)

**Переменные:** `up_bid_[1-5]_price`, `up_bid_[1-5]_size`, `up_ask_[1-5]_price`, `up_ask_[1-5]_size`, `down_bid_[1-5]_price`, `down_bid_[1-5]_size`, `down_ask_[1-5]_price`, `down_ask_[1-5]_size`

**Описание графика:**
Два параллельных стакана (UP и DOWN), каждый как горизонтальная столбчатая диаграмма. Слева — биды (зелёные полосы, длина = size), справа — аски (красные полосы). Ось Y — уровни цен (5 уровней). Альтернативно — heatmap по времени, где яркость ячейки = объём на данном ценовом уровне в данный момент.

**Торговый контекст:**
- Толстые биды, тонкие аски → покупательское давление
- Тонкие биды, толстые аски → давление продавцов
- Большой ордер на одном уровне → "стена" (support/resistance)
- Формат: `цена→размер` (1 контракт = $1 при выигрыше)

**Особенности:**
- Цены контрактов в диапазоне 0–1
- Может быть полностью пустым с одной стороны — нормально для начала рынка
- Подсвечивать уровни с аномально большими ордерами (>2× средний размер)

---

## Группа 8: Глубина, баланс и арбитраж (Depth, Imbalance & Arbitrage)

**Переменные:** `pm_up_bid_depth5`, `pm_up_ask_depth5`, `pm_up_total_depth5`, `pm_down_bid_depth5`, `pm_down_ask_depth5`, `pm_down_total_depth5`, `pm_up_imbalance`, `pm_down_imbalance`

**Описание графика:**
Три подграфика. **Верхний** — stacked area chart: глубина бидов vs асков для UP и DOWN рынков (4 area). Показывает общий объём ликвидности. **Средний** — осциллятор imbalance [-1, +1] для UP и DOWN (2 линии). **Нижний** — арбитражный индикатор: линия суммы лучших бидов UP + DOWN на каждом уровне (L1–L5), горизонтальная линия 1.0 — если сумма < 1.0, есть арбитражная возможность (edge = 1.0 - сумма).

**Торговый контекст (Imbalance):**
- `Imbalance > +0.3` → сильное давление покупателей (bid pressure)
- `Imbalance < -0.3` → сильное давление продавцов (ask pressure)
- `|Imbalance| < 0.1` → сбалансированный стакан
- Резкое изменение Imbalance — опережающий индикатор движения цены
- **UP Imbalance > +0.3** → рынок ожидает рост BTC → покупай UP
- **DOWN Imbalance > +0.3** → рынок ожидает падение → покупай DOWN

**Торговый контекст (Арбитраж):**
- `UP_Bid + DOWN_Bid < 1.0` → edge > 0, арбитражная возможность (продать оба контракта)
- `Edge > 0.02 (2%+)` при достаточной глубине → **СИГНАЛ арбитража**
- `Edge ≈ 0` → рынок эффективен
- `Edge < 0` → "переплата" за ликвидность (нормально)

**Особенности:**
- Ось Y верхняя: контракты
- Ось Y средняя: безразмерный [-1, +1], пороги ±0.1 и ±0.3
- Ось Y нижняя: цена (0–1), горизонтальная линия 1.0
- Imbalance может быть пустым если одна сторона = 0

---

## Группа 9: Микроструктура стакана (Spread, Microprice, Slope)

**Переменные:** `pm_up_spread`, `pm_down_spread`, `pm_up_microprice`, `pm_down_microprice`, `pm_up_bid_slope`, `pm_up_ask_slope`, `pm_down_bid_slope`, `pm_down_ask_slope`

**Описание графика:**
Три подграфика. **1) Spread** — линии спреда для UP и DOWN. **2) Microprice** — взвешенная "справедливая цена" для UP и DOWN контрактов (более точная оценка вероятности исхода чем простой mid-price). **3) Slope** — 4 линии (bid/ask для UP/DOWN).

**Торговый контекст (Spread):**
- `Spread < 0.02` → ликвидный рынок, можно торговать market orders
- `Spread 0.02–0.05` → нормальная ликвидность
- `Spread > 0.05` → низкая ликвидность, используй лимитные ордера
- `Spread > 0.10` → критически низкая ликвидность, **избегай входа**
- Спред расширяется к концу 15-минутного периода
- Спред сужается после крупных сделок (маркет-мейкеры подтягивают ордера)

**Торговый контекст (Microprice):**
- Microprice > Mid → покупательское давление (бычий сигнал на данном контракте)
- Microprice < Mid → давление продавцов
- Если Microprice UP растёт → ожидай рост цены UP контракта
- Microprice близка к ask → скоро пробой вверх

**Торговый контекст (Slope):**
- `Slope > 0.6` → "стена" — ликвидность сконцентрирована на лучших уровнях
- `Slope < 0.4` → распределённая ликвидность, глубокий стакан
- `Slope ≈ 0.5` → нормальное распределение
- **High Bid Slope + Low Ask Slope** → сильная поддержка, слабое сопротивление → BULLISH
- **Low Bid Slope + High Ask Slope** → слабая поддержка, сильное сопротивление → BEARISH

**Особенности:**
- Spread: ось Y = цена контракта (0–1), пороги 0.02, 0.05, 0.10
- Microprice: ось Y = цена контракта (0–1)
- Slope: ось Y = [0, 1], горизонтальные пороги 0.4 и 0.6

---

## Группа 10: Потребление ликвидности (Eat-Flow)

**Переменные:** `pm_up_bid_eatflow`, `pm_up_ask_eatflow`, `pm_down_bid_eatflow`, `pm_down_ask_eatflow`

**Описание графика:**
Два осциллятора (UP и DOWN). Каждый показывает скорость изменения глубины бидов и асков (контракты/секунду). Один из самых важных индикаторов — показывает реальную активность тейкеров, а не просто размещение ордеров.

**Торговый контекст и пороги:**
- `Eat-Flow < -50` → биды/аски активно съедаются — агрессивные тейкеры
- `Eat-Flow > +50` → ликвидность добавляется — маркет-мейкеры
- `|Eat-Flow| < 10` → стабильный стакан
- **UP Ask Eat-Flow << 0** → кто-то агрессивно покупает UP → **СИГНАЛ UP**
- **UP Bid Eat-Flow << 0** → кто-то агрессивно продаёт UP → **СИГНАЛ DOWN**
- **Bid Eat-Flow негативный, но Imbalance растёт** → маркет-мейкер восполняет ликвидность (ложный сигнал)
- Комбинировать с Slope: "стена" (Slope > 0.6) + отрицательный Eat-Flow = стена пробита

**Особенности:**
- Нулевая линия обязательна
- Ось Y: контракты/секунду
- Пороги: ±10 (пунктир), ±50 (выделенная зона)
- Красный цвет для отрицательных значений (ликвидность "съедается")
- Зелёный для положительных (ликвидность добавляется)

---

## Группа 11: Композитные торговые сигналы (Signal Overlay)

**Переменные:** Комбинация из всех групп (рассчитывается на лету).

**Описание:**
Не отдельный график, а система визуальных маркеров (вертикальные линии / иконки / бейджи), которые отображаются поверх всех графиков при выполнении определённых условий. Каждый сигнал — комбинация нескольких переменных.

**Сигнал 1 — Momentum Breakout (🟢/🔴 стрелка):**
- `|Ret1s| > 0.10` + `Volume Spike > 3.0` + `UP/DOWN Imbalance > +0.2`
- Действие: Купить UP (при Ret1s > 0) или DOWN (при Ret1s < 0) по market

**Сигнал 2 — Fade Extreme Move (⚠️ предупреждение):**
- `|Ret5s| > 0.30` (сильное движение) + `Volume Spike снижается (<1.5)` + `Imbalance начинает разворачиваться`
- Действие: Ставка против движения (fade)

**Сигнал 3 — Orderbook Pressure (📊 давление):**
- `Eat-Flow < -100` (биды/аски активно съедаются) + `Slope > 0.7` (стена) + `Spread сужается`
- Действие: Ожидать пробой или отскок от стены

**Сигнал 4 — Arbitrage (💰 арбитраж):**
- `UP_Bid_L1 + DOWN_Bid_L1 < 0.98` (Edge > 2%) + достаточная глубина на обоих рынках
- Действие: Продать UP + DOWN по лучшим бидам

**Особенности:**
- Каждый сигнал — вертикальная линия на всех синхронизированных графиках
- Цвет: зелёный (UP), красный (DOWN), жёлтый (предупреждение), фиолетовый (арбитраж)
- Тултип сигнала: название, условия, рекомендация

---

## Временные фазы 15-минутного рынка (фон оси X)

Вся область графика делится на 4 зоны по оси X (seconds_till_end), каждая с полупрозрачным фоном:

| Фаза | Время | seconds_till_end | Фон | Характеристика |
|-------|-------|-----------------|------|----------------|
| Открытие | 0–3 мин | 900–720 | Зелёный 5% | Высокая волатильность, широкие спреды. Ждать стабилизации |
| Формирование | 3–10 мин | 720–300 | Нет (чистый) | Спреды сужаются, тренд определяется. Основная фаза входа |
| Развязка | 10–14 мин | 300–60 | Жёлтый 5% | Цены сходятся к 0/1. Фиксация прибыли |
| Финал | последние 60с | 60–0 | Красный 10% | Ликвидность минимальна, спреды максимальны. НЕ ВХОДИТЬ |

---

## Общие технические требования

1. **Ось X** — `seconds_till_end` (900→0), подписи в формате MM:SS, с фоновыми зонами фаз рынка
2. **Обновление** — если дашборд live, обновлять каждую секунду; если аналитика — загружать CSV целиком
3. **Синхронизация** — все графики синхронизированы по времени; при наведении на один, курсор появляется на всех (crosshair sync)
4. **Тултипы** — при наведении показывать все значения группы с единицами измерения и торговой интерпретацией (например, "Spike: 3.5 — ЗНАЧИТЕЛЬНЫЙ ВСПЛЕСК")
5. **Пустые значения** — корректно обрабатывать пропуски (не ломать линию, а прерывать); для оракула показывать плашку "нет данных"
6. **Цветовая схема** — тёмная тема, зелёный/красный для бычьих/медвежьих сигналов
7. **Библиотека** — рекомендуется Lightweight Charts (TradingView), Recharts, или Plotly
8. **Адаптивность** — графики должны масштабироваться под ширину экрана; на мобильных — вертикальный стек
9. **Пороговые линии** — на каждом графике горизонтальные пунктирные линии на ключевых уровнях (описаны в каждой группе)
10. **Сигнальные маркеры** — композитные сигналы (Группа 11) отображаются вертикальными линиями поверх всех графиков

---

## Чек-лист проверки перед входом в сделку (sidebar widget)

Опциональный sidebar-виджет с автоматической проверкой условий:

| Условие | Переменная | Порог |
|---------|-----------|-------|
| ✅ Достаточно времени? | `seconds_till_end` | > 120s |
| ✅ Ликвидный рынок? | `pm_up_spread` / `pm_down_spread` | < 0.05 |
| ✅ Imbalance подтверждает? | `pm_up_imbalance` / `pm_down_imbalance` | > +0.2 в направлении |
| ✅ Моментум не противоречит? | `ret1s` и `ret5s` | одного знака |
| ✅ Объём подтверждает? | `binance_volume_spike` | > 2.0 |
| ✅ P/VWAP согласован? | `p_vwap_5s` и `p_vwap_30s` | одного знака |
| ✅ Eat-Flow не против? | eat-flow | не разворачивается |
| ✅ Нет стены против позиции? | slope | < 0.6 на стороне против |
